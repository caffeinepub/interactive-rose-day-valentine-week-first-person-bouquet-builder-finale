/**
 * Constants for bouquet layout and vase dimensions
 */

// Vase opening radius (matches the vase rim in Vase.tsx)
export const VASE_OPENING_RADIUS = 0.38;

// Vertical spacing between rose layers
export const LAYER_HEIGHT_SPACING = 0.12;

// Maximum tilt angle for roses (in radians)
export const MAX_TILT = 0.3;

// Spawn position offset for rose entrance animation
export const SPAWN_OFFSET_Y = 2.0;
export const SPAWN_OFFSET_Z = 1.5;
